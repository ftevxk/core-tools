<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="java.util.Arrays" />

        <import type="android.graphics.Color" />

        <variable
            name="model"
            type="com.ftevxk.example.MainItemModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="2dp"
        android:background="@android:color/white"
        android:gravity="center"
        android:padding="10dp">

        <Button
            android:id="@+id/button"
            android:layout_width="100dp"
            android:layout_height="40dp"
            android:gravity="center"
            android:text="@{model.title}"
            android:textColor="@android:color/white"
            tools:pressed_shape_radius="@{Arrays.asList(30f, 30f, 0, 0)}"
            tools:pressed_shape_solid_color="@{Color.BLUE}"
            tools:pressed_shape_type="@{`rect`}"
            tools:shape_dash_gap="@{5}"
            tools:shape_dash_width="@{10}"
            tools:shape_radius="@{30}"
            tools:shape_solid_color="@{@color/colorPrimary}"
            tools:shape_stroke_color="@{Color.BLUE}"
            tools:shape_stroke_width="@{2}"
            tools:shape_type="@{`rect`}"
            tools:unit="@{`dp`}" />
    </RelativeLayout>
</layout>